# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=15433
POSTGRES_DB=broader_impacts_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# Keycloak Settings
# Using shared Civic OS Keycloak instance (auth.civic-os.org)
# For production or custom RBAC, run your own Keycloak (see docs/AUTHENTICATION.md)
KEYCLOAK_URL=https://auth.civic-os.org
KEYCLOAK_REALM=civic-os-dev
KEYCLOAK_CLIENT_ID=myclient

# MinIO / S3 Configuration (optional - defaults provided)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_BUCKET=civic-os-files
