import { PropToTitlePipe } from './prop-to-title.pipe';

describe('PropToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new PropToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
